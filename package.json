{
  "name": "todo-monorepo",
  "private": true,
  "scripts": {
    "build": "pnpm --filter backend run build",
    "start": "pnpm --filter backend run start",
    "start:dev": "pnpm --filter backend run start:dev",
    "test": "pnpm --filter backend run test",
    "lint": "pnpm --filter backend run lint"
  }
}
